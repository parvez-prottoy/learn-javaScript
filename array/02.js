/**
 * ?@Title: Initialize Array - Constructor vs Factory
 */
// ! Constructor
const a1 = new Array(); // []
const a2 = new Array(5); // a2 arr data limit 5
// ! Factory
const b1 = Array(); // []
const b2 = Array(5); // a2 arr data limit 5
